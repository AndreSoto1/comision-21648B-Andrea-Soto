<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/public/style.css">
    <link rel="stylesheet" href="/public/bootstrap.css">
    <link rel="stylesheet" href="/public/bootstrap.js">
    <title>Blog: <%= post.titulo_post %> </title>
</head>
<body>
    <h1><%= post.titulo_post %></h1>
    <div style="width: 50%;">
        <img src="<%= post.link %>" alt="<%= post.titulo_post %>">
    </div>
    <p><%= post.contenido %></p>
    <span>Creado:<%= post.createdAt</span>
    <span>Ultima Modificacion: <%= post.updatedAt</span>
    <form method="GET" action="/editar/<%= post.id %>">
        <button type="submit">Editar</button>
    </form>
    <form method="POST" action="/eliminar/<%= post.id %>">
        <button type="submit">Eliminar</button>
    </form>
    <form action="/" method="GET">
        <button type="submit">Volver al inicio</button>
    </form>
</body>
</html>
