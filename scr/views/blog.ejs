<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/style.css">
    <link rel="stylesheet" href="/bootstrap.css">
    <title>Blog: <%= post.titulo_post %> </title>
</head>
<body>
    <div class="container">
        <h1 class="titulos-todos"><%= post.titulo_post %></h1>
        <div class="row">
            <div class="col-md-8">
                <img src="<%= post.link %>" alt="<%= post.titulo_post %>" class="img-fluid" style="max-width: 100%";>
            </div>
            <div class="col-md-4">
                <p><%= post.contenido %></p>
                <span class="post-contenido">Creado: <%= post.createdAt %></span>
                <span class="post-contenido">Última Modificación: <%= post.updatedAt %></span>
                <div class="d-flex justify-content-between mt-3">
                    <form method="get" action="/post/editar/<%= post.id %>">
                        <button type="submit" class="btn btn-primary">Editar</button>
                    </form>
                    <form method="get" action="/post/eliminar/<%= post.id %>">
                        <button type="submit" class="btn btn-danger">Eliminar</button>
                    </form>
                    <form action="/post" method="GET">
                        <button type="submit" class="btn btn-secondary">Inicio</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
    
    <script src="/bootstrap.js"></script>
</body>
</html>
